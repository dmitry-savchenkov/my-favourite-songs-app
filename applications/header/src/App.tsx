import React, { JSX } from "react";
import styles from './app.module.css';

const App = (): JSX.Element => {
  return <header className={styles.header}>Best place to see your favourite songs 🥰</header>;
};

export default App;